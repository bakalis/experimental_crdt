version: "3.9"

x-s3-env: &s3-env
  S3_ENDPOINT: ${S3_ENDPOINT}
  S3_BUCKET: ${S3_BUCKET}
  S3_REGION: ${S3_REGION}
  S3_ACCESS_KEY: ${AWS_ACCESS_KEY_ID}
  S3_SECRET_KEY: ${AWS_SECRET_ACCESS_KEY}
  METRICS_FILE_PATH: ${METRICS_FILE_PATH}

services:
  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio
    restart: unless-stopped

    command: server /data --console-address ":9001"

    ports:
      - "9000:9000" # S3 API
      - "9001:9001" # Console

    env_file:
      - .env

    volumes:
      - minio-data:/data

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 20s
      retries: 3

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: cadvisor
    ports:
      - "8080:8080"
    privileged: true
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro

  # GC replica — S1
  gc-server-1:
    build:
      context: .
      dockerfile: server.dockerfile
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: gc-server-1
      LISTEN_HOST: gc-server-1
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-1,normal-server-5,normal-server-6,normal-server-9,normal-server-11,normal-server-15,gc-server-2,gc-server-3,gc-server-4,gc-server-5
      GC_REPLICA: "true"
    volumes:
      - ./logs/gc_server_1/:/logs/
    ports:
      - "9110:9000"
      - "9111:9100"
    depends_on:
      minio:
        condition: service_healthy

  # GC replica — S2
  gc-server-2:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: gc-server-2
      LISTEN_HOST: gc-server-2
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-1,normal-server-2,normal-server-7,normal-server-10,normal-server-11,normal-server-12,gc-server-1,gc-server-3,gc-server-4,gc-server-5
      GC_REPLICA: "true"
    volumes:
      - ./logs/gc_server_2/:/logs/
    ports:
      - "9120:9000"
      - "9121:9100"
    depends_on:
      minio:
        condition: service_healthy

  # GC replica — S3
  gc-server-3:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: gc-server-3
      LISTEN_HOST: gc-server-3
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-2,normal-server-3,normal-server-6,normal-server-8,normal-server-12,normal-server-13,gc-server-1,gc-server-2,gc-server-4,gc-server-5
      GC_REPLICA: "true"
    volumes:
      - ./logs/gc_server_3/:/logs/
    ports:
      - "9130:9000"
      - "9131:9100"
    depends_on:
      minio:
        condition: service_healthy

  # GC replica — S4
  gc-server-4:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: gc-server-4
      LISTEN_HOST: gc-server-4
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-3,normal-server-4,normal-server-7,normal-server-9,normal-server-13,normal-server-14,gc-server-1,gc-server-2,gc-server-3,gc-server-5
      GC_REPLICA: "true"
    volumes:
      - ./logs/gc_server_4/:/logs/
    ports:
      - "9140:9000"
      - "9141:9100"
    depends_on:
      minio:
        condition: service_healthy

  # GC replica — S5
  gc-server-5:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: gc-server-5
      LISTEN_HOST: gc-server-5
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-4,normal-server-5,normal-server-8,normal-server-10,normal-server-14,normal-server-15,gc-server-1,gc-server-2,gc-server-3,gc-server-4
      GC_REPLICA: "true"
    volumes:
      - ./logs/gc_server_5/:/logs/
    ports:
      - "9150:9000"
      - "9151:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N1
  normal-server-1:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-1
      LISTEN_HOST: normal-server-1
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-2,normal-server-15,gc-server-1,gc-server-2
    volumes:
      - ./logs/normal_server_1/:/logs/
    ports:
      - "9210:9000"
      - "9211:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N2
  normal-server-2:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-2
      LISTEN_HOST: normal-server-2
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-1,normal-server-3,gc-server-2,gc-server-3
    volumes:
      - ./logs/normal_server_2/:/logs/
    ports:
      - "9220:9000"
      - "9221:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N3
  normal-server-3:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-3
      LISTEN_HOST: normal-server-3
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-2,normal-server-4,gc-server-3,gc-server-4
    volumes:
      - ./logs/normal_server_3/:/logs/
    ports:
      - "9230:9000"
      - "9231:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N4
  normal-server-4:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-4
      LISTEN_HOST: normal-server-4
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-3,normal-server-5,gc-server-4,gc-server-5
    volumes:
      - ./logs/normal_server_4/:/logs/
    ports:
      - "9240:9000"
      - "9241:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N5
  normal-server-5:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-5
      LISTEN_HOST: normal-server-5
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-4,normal-server-6,gc-server-1,gc-server-5
    volumes:
      - ./logs/normal_server_5/:/logs/
    ports:
      - "9250:9000"
      - "9251:9100"
    depends_on:
      minio:
        condition: service_healthy

  #Normal node — N6
  normal-server-6:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-6
      LISTEN_HOST: normal-server-6
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-5,normal-server-7,gc-server-1,gc-server-3
    volumes:
      - ./logs/normal_server_6/:/logs/
    ports:
      - "9260:9000"
      - "9261:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N7
  normal-server-7:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-7
      LISTEN_HOST: normal-server-7
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-6,normal-server-8,gc-server-2,gc-server-4
    volumes:
      - ./logs/normal_server_7/:/logs/
    ports:
      - "9270:9000"
      - "9271:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N8
  normal-server-8:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-8
      LISTEN_HOST: normal-server-8
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-7,normal-server-9,gc-server-3,gc-server-5
    volumes:
      - ./logs/normal_server_8/:/logs/
    ports:
      - "9280:9000"
      - "9281:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N9
  normal-server-9:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-9
      LISTEN_HOST: normal-server-9
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-8,normal-server-10,gc-server-1,gc-server-4
    volumes:
      - ./logs/normal_server_9/:/logs/
    ports:
      - "9290:9000"
      - "9291:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N10
  normal-server-10:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-10
      LISTEN_HOST: normal-server-10
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-9,normal-server-11,gc-server-2,gc-server-5
    volumes:
      - ./logs/normal_server_10/:/logs/
    ports:
      - "9300:9000"
      - "9301:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N11
  normal-server-11:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-11
      LISTEN_HOST: normal-server-11
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-10,normal-server-12,gc-server-1,gc-server-2
    volumes:
      - ./logs/normal_server_11/:/logs/
    ports:
      - "9310:9000"
      - "9311:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N12
  normal-server-12:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-12
      LISTEN_HOST: normal-server-12
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-11,normal-server-13,gc-server-2,gc-server-3
    volumes:
      - ./logs/normal_server_12/:/logs/
    ports:
      - "9320:9000"
      - "9321:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N13
  normal-server-13:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-13
      LISTEN_HOST: normal-server-13
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-12,normal-server-14,gc-server-3,gc-server-4
    volumes:
      - ./logs/normal_server_13/:/logs/
    ports:
      - "9330:9000"
      - "9331:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N14
  normal-server-14:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-14
      LISTEN_HOST: normal-server-14
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-13,normal-server-15,gc-server-4,gc-server-5
    volumes:
      - ./logs/normal_server_14/:/logs/
    ports:
      - "9340:9000"
      - "9341:9100"
    depends_on:
      minio:
        condition: service_healthy

  # Normal node — N15
  normal-server-15:
    image: crdt-server:latest
    restart: unless-stopped
    networks:
      - crdt-net
    environment:
      <<: *s3-env
      NODE_NAME: normal-server-15
      LISTEN_HOST: normal-server-15
      LISTEN_PORT: "9000"
      CLIENT_PORT: "9100"
      DISCOVERY_CONNECT_NODE_IDS: normal-server-1,normal-server-14,gc-server-1,gc-server-5
    volumes:
      - ./logs/normal_server_15/:/logs/
    ports:
      - "9350:9000"
      - "9351:9100"
    depends_on:
      minio:
        condition: service_healthy

  gc-client-1:
    build:
      context: .
      dockerfile: client.dockerfile
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: gc-server-1:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/gc_client_1/:/logs/
    depends_on:
      - gc-server-1

  gc-client-2:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: gc-server-2:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/gc_client_2/:/logs/
    depends_on:
      - gc-server-2

  gc-client-3:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: gc-server-3:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/gc_client_3/:/logs/
    depends_on:
      - gc-server-3

  gc-client-4:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: gc-server-4:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/gc_client_4/:/logs/
    depends_on:
      - gc-server-4

  gc-client-5:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: gc-server-5:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/gc_client_5/:/logs/
    depends_on:
      - gc-server-5

  normal-client-1:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-1:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_1/:/logs/
    depends_on:
      - normal-server-1

  normal-client-2:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-2:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_2/:/logs/
    depends_on:
      - normal-server-2

  normal-client-3:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-3:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_3/:/logs/
    depends_on:
      - normal-server-3

  normal-client-4:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-4:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_4/:/logs/
    depends_on:
      - normal-server-4

  normal-client-5:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-5:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_5/:/logs/
    depends_on:
      - normal-server-5

  normal-client-6:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-6:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_6/:/logs/
    depends_on:
      - normal-server-6

  normal-client-7:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-7:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_7/:/logs/
    depends_on:
      - normal-server-7

  normal-client-8:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-8:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_8/:/logs/
    depends_on:
      - normal-server-8

  normal-client-9:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-9:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_9/:/logs/
    depends_on:
      - normal-server-9

  normal-client-10:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-10:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_10/:/logs/
    depends_on:
      - normal-server-10

  normal-client-11:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-11:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_11/:/logs/
    depends_on:
      - normal-server-11

  normal-client-12:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-12:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_12/:/logs/
    depends_on:
      - normal-server-12

  normal-client-13:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-13:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_13/:/logs/
    depends_on:
      - normal-server-13

  normal-client-14:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-14:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_14/:/logs/
    depends_on:
      - normal-server-14

  normal-client-15:
    image: crdt-client:latest
    networks:
      - crdt-net
    environment:
      SERVER_ADDR: normal-server-15:9100
      TOTAL_REQUESTS: 50
      SLEEP_MS: 0
      METRICS_FILE_PATH: ${METRICS_FILE_PATH}
    volumes:
      - ./logs/normal_client_15/:/logs/
    depends_on:
      - normal-server-15

volumes:
  minio-data:

networks:
  crdt-net:
    driver: bridge
